# ദി◝ ⩊ ◜)

cmake -B build  <br />
cmake --build build  <br />
./build/basiCompiler test.txt  <br />

keep in mind that this is actually an interpreter (currently)
The only commands that are possible are PRINT arithmetic (only + or *) and PRINT STRING.
I don't really plan on extending this as this was just a exercise to learn how compilers (interpreters) work
