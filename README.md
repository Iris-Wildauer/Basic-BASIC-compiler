# ദി◝ ⩊ ◜)

cmake -B build. 
cmake --build build. 
./build/basiCompiler test.txt. 

keep in mind that this is actually an interpreter and also it can only evaluate one statement in one line
(as of now)
